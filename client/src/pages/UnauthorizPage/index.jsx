import { Typography } from '@mui/material';
import React from 'react';

const UnauthorizPage = () => (
  <Typography variant="h3">You should sign in to view this page</Typography>
);

export default UnauthorizPage;
